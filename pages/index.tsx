import type { NextPage } from 'next';

const Home: NextPage = () => {
	return (
		<div className="flex h-full w-full items-center justify-center">
			<h1>Home area</h1>
		</div>
	);
};

export default Home;
